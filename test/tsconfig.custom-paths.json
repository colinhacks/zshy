{
  "description": "Test: Separate declaration directory configuration. Tests zshy's support for declarationDir different from outDir. outDir: build, declarationDir: types, should generate proper exports map",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "moduleResolution": "node",
    "outDir": "./build",
    "rootDir": ".",
    "declarationDir": "./types"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "build", "types"]
}
