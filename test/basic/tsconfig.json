{
  "description": "Test: Basic configuration with default settings. This tests the most common use case with minimal configuration. outDir: dist, rootDir: src, standard ES2020 target",
  "extends": "../tsconfig.default.json",
  "compilerOptions": {
    "outDir": "./dist",
    "composite": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}